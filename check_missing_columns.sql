-- Check existing columns in tb_category
SHOW COLUMNS FROM tb_category;

-- Check existing columns in tb_post  
SHOW COLUMNS FROM tb_post;

-- Check existing columns in tb_user
SHOW COLUMNS FROM tb_user;
